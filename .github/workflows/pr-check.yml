name: Disabled CI

on:
  push:
    branches:
      - "a-branch-that-will-never-exist-999"

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI workflow is disabled in this fork."
