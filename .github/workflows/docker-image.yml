name: Disabled Docker Image

on:
  push:
    branches:
      - "a-branch-that-will-never-exist-999"

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docker image workflow is disabled in this fork."
